1.write the pseudocode for calculating the total cost of items in a shopping cart
  including applying a discount if the total exceeds a certain amount
              item price = double [12.00, 34.25, 56.85, 45, 23.64]
          double discount=0
          double total cost=0
          for i in item price;
          		total cost= i + Total cost
          if total cost>150;
          	double discount =(total cost*10)/100
          	double total cost= total cost - discount
          	double print total cost
          else ;
          	double print total cost

2.simple calculator
        a= double input first number
        b= double input second number
        enter the operator(+, -, *, /)
        
        if operator = +
        	double print 'result=',a + b
        else if operator = -
        	double print 'result=',a - b
        else if operator = *
        	double print 'result=',a * b
        else if operator = /
        	double print 'result=',a / b 
        else
        	print 'invalid'

3.FizzBuzz Problem
        for i in range(0,101):
        	if i % 3 == 0; and if i % 5 == 0:
        		print 'fizz buzz'
        	elif i % 3 == 0;
        		print 'fizz'
        	elif i % 5 == 0;
        		print 'buzz'
        	else
        		print i
        	end if
        end for

4.check string is palindrome or not
        n = input  a string
        y=''
        for i in n
        	y = i + y
        if y==n
        	print 'string is palindrome'
        else 
        	print 'not palindrome'

5.finding largest from araay
      a= integer[2, 4, 8, 66, 44, 96]
      c=0
      for i in a
        if i>c
          c==i
        else
          continue
      integer print c

6.write pseudocode to manage a simple bank account that allows deposits withdrawals and checking the balance.
      l=[]
      Function Register()
      	l1=list[]
      		integer a=enter account number
      		l1.append(a)
      		double balance=0
      		l1.append(balance)
      //append the account number and into the list if you are first time registering in a bank.
      
      Function login()
      	integer b=enter account number
      	for i in l:
      		if i[0]==b
      			enter option string (deposit, withdrawal, check balance)
      			if choice=deposit
      				integer d = enter amount
      				for i in l:
      					i[1] = i[1] + d	
      			elif choice=withdrawal
      				integer d = enter amount
      				for i in l:
      					i[1] = i[1] - d	
      			elif choice=check balance
      				print balance
      			else 
      				print 'invalid choice'
      		else
      			print 'invalid account number'

7.Sum of first 10 even number.
    BEGIN
    declare a integer sum=0
    declare a integer number= 2
    declare a integer count=0
    
    While(count<10)
      sum= sum + number
      number=number+2
      count=count+1
    END

8. To print the one is adult based his/her age 
   Note: Dont print if not adult
    function IsAdult(age) -> boolean 
        isAdult = (age >= 18);
        return isAdult; 

    function TestIsAdult(): -> None
        Input age 
        if(IsAdult(age)) 
            print 'You are an adult.'
        end if

9.Take a num 10, if num greater than 5 ,print "num as 'greater than 5'"

      pseudo:rountine 
      function IsGreaterThan5(num)
      IsGreaterThan5 = (num > 5);
      return IsGreaterThan5;
      
      pseudo:routine test
      function TestIsGreaterThan5()
      Input num 
      if(IsGreaterThan5(num)) 
        print 'Number is greater than Five.'
      end if

10.To print 'number is positive' if num > 0,it should not print if not positive 

      pseudo:rountine 
      function IsPositiveNum(num)
      IsPositiveNum= (num > 0);
      return IsPositiveNum;
      
      pseudo:routine test
      function TestIsPositiveNum()
      Input num 
      if(IsPositiveNum(num))
        print 'Number is positive.'
      end if

11.if temp > 25, print it is hot outside.

    pseudo:rountine 
    function CheckTemp(temp)
    CheckTemp= (temp > 25);
    return CheckTemp;
    
    pseudo:routine test
    function TestCheckTemp()
    Input temp
    if(CheckTemp(temp))
      print 'It is hot outside.'
    end if

12.Input mark,if mark is greater than 40 print pass else fail

    pseudocode:routine
    function IsMarkGreaterThan40(mark)
        return (mark > 40)
    
    function IsMarkGreaterThan40(mark)
        isMarkGreaterThan40 = (mark > 40)
        return isMarkGreaterThan40
    
    pseudocode:test routine
    function TestIsMarkGreaterThan40()
        Input mark 
        if IsMarkGreaterThan40(mark)
            print "pass" 
        else 
            print "fail"

13.To check the person is teenageer,adult or child
    
    pseudocode:routine
    function IsAdult(age)
            isAdult = (age >= 18);
            return isAdult; 
        function IsTeenager(age)
            isTeenager = (age >= 13);
            return isTeenager; 
    pseudocode:test
        function TestIsAdultTeenagerOrChild():
            Input age 
            if(IsAdult(age)) 
                print 'You are an adult.'
            else if(IsTeenager(age)) 
                print 'You are a teenager.'
            else
                print 'You are a child.'
            end if

14.check for car maintenance
      pseudocode:routine
      function SuggestMaintenance(serviceOption->char) -> string
              string suggestion;
              switch serviceOption
                  case 'O':
                      suggestion = "Change Oil and Replace Oil Filter"
                      break
                  case 'T':
                      suggestion = "Rotate Tire and align them properly"
                      break
                  case 'B':
                      suggestion = "Inspect the brake and replace them if necessary"
                      break
                  case 'A':
                      suggestion = "Battery Check and Replace them"
                      break
                  default: 
                      suggestion = "Some other reason"
                      break 
              end switch 
              return suggestion

        pseudocode:routine test
        function TestSuggestMaintenance() 
             Input serviceOption 
             suggestion = SuggestMaintenance(serviceOption)
             print suggestion
